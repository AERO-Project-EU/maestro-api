# Repository

The relational persistence layer: JPA entities and their Spring Data DAOs. A library jar with no
configuration of its own, the datasource is defined by the application that uses it (the backend).

## Build

```
mvn -pl repository install
```
